<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="comp3350.ims">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".edit_dialog_inventory_row"
            android:exported="false" />
        <activity
            android:name=".presentation.ItemCreateActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.HomeActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.ViewAllActivity"
            android:label="@string/title_activity_courses">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="comp3350.ims.presentation.HomeActivity" />
        </activity>
        <activity
            android:name=".presentation.ActiveInventoryActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.CategoryActivity" />
        <activity android:name=".presentation.LocationActivity" />
    </application>

</manifest>