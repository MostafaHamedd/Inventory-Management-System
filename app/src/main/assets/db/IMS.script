CREATE SCHEMA PUBLIC AUTHORIZATION DBA
CREATE MEMORY TABLE INVENTORY(INVENTORYID VARCHAR(8) NOT NULL PRIMARY KEY)
CREATE MEMORY TABLE CATEGORY(NAME VARCHAR(50) NOT NULL PRIMARY KEY)
CREATE MEMORY TABLE LOCATION(NAME VARCHAR(50) NOT NULL PRIMARY KEY)
CREATE MEMORY TABLE ITEMTYPE(ID INT NOT NULL PRIMARY KEY, NAME VARCHAR(50), INVENTORYID VARCHAR(8), PRICE DECIMAL(10,2), CATEGORYNAME VARCHAR(50), LOCATIONNAME VARCHAR(50),DATE VARCHAR(50), QUANTITY INTEGER, CONSTRAINT CONSTRAINT2 FOREIGN KEY(CATEGORYNAME) REFERENCES CATEGORY(NAME), CONSTRAINT CONSTRAINT3 FOREIGN KEY(LOCATIONNAME) REFERENCES LOCATION(NAME), CONSTRAINT CONSTRAINT4 FOREIGN KEY(INVENTORYID) REFERENCES INVENTORY(INVENTORYID))
CREATE MEMORY TABLE ITEM(ID INT NOT NULL PRIMARY KEY, ITEMTYPEID INT, DATE VARCHAR(50), LOCATIONNAME VARCHAR(50), CONSTRAINT CONSTRAINT5 FOREIGN KEY(LOCATIONNAME) REFERENCES LOCATION(NAME),CONSTRAINT CONSTRAINT6 FOREIGN KEY(ITEMTYPEID) REFERENCES ITEMTYPE(ID))
CREATE USER IMS PASSWORD ""
GRANT DBA TO IMS
SET WRITE_DELAY 20
SET SCHEMA PUBLIC
INSERT INTO INVENTORY VALUES('MAIN')